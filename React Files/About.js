import React from "react";

// This is a simple component that displays a heading
function About() {
  // The component returns JSX (similar to HTML) 
  // which React will render on the UI
  return <h2>About the Income Tax Department</h2>;
}

// imported and used in other files (like App.js)
export default About;
